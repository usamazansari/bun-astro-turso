import { Blockquote } from "@mantine/core";

export function ModalFromCard() {
  return (
    <Blockquote color="blue" cite="Forrest Gump" className="mt-4">
      Life is like an npm install - you never know what you are going to get.
    </Blockquote>
  );
}
