---
import DateTimeFormatter from '../../components/astro/BlogDateTimeFormatter.astro';
import Layout from '../Layout.astro';

interface Props {
  frontmatter: { title: string; author: string; publishDate: string; createdAt: string };
}

const {
  frontmatter: { title, publishDate, createdAt, author },
} = Astro.props;
---

<Layout>
  <article class="mx-auto border border-color-border-default rounded my-8 p-4 prose lg:prose-lg dark:prose-invert">
    <!-- <div class="flex flex-col gap-4"> -->
    <!-- <h2>{title}</h2> -->
    <slot />
    <!-- <div class="flex flex-col gap-2">
        <span>{author}</span>
        <DateTimeFormatter date={publishDate || createdAt} />
      </div> -->
    <!-- <div class="-mx-4 p-px bg-color-border-default"></div> -->
    <!-- </div> -->
  </article>
</Layout>
